# Framework Core

This layer gives `@claw/core` a plugin-first runtime with:

- `AgentBuilder` for composition
- `AgentRuntime` for execution
- `FrameworkPlugin` for capability injection
- `FrameworkEventBus` for lifecycle visibility
- `OpenClawBridge` for tool/skill interoperability
- `zeroGPlugin` for quickly wiring storage and compute
