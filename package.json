{
  "name": "claw-machine",
  "version": "0.5.0",
  "description": "Self-improving agent framework on 0G — @claw/core SDK, plugins, CLI, React hooks",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "install-all": "npm install && npm install --workspace=backend && npm install --workspace=frontend",
    "start": "npm run start --workspace=backend",
    "test:e2e": "playwright test",
    "test:e2e:auth": "playwright test tests/e2e/auth-flow.spec.ts",
    "test:e2e:security": "playwright test tests/e2e/security.spec.ts",
    "test:e2e:smoke": "playwright test --grep @smoke",
    "demo": "npx tsx examples/frameworkDemo.ts",
    "packages:build": "cd packages/core && tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "concurrently": "^8.2.0"
  }
}